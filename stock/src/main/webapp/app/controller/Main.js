Ext.define('StockBoard.controller.Main', {
    extend: 'Ext.app.Controller',
    /*views: [
            'Viewport','SectorGrid'],
     stores:['StoreStock'],
     models:['StockResponse'],
    	
    	 init:function(){
    	    	var me = this;
    	    	this.control({
    	    		//when the viewport is rendered
                    'viewport > panel' : {
                        render : this.onPanelRendered
                    },
    	    		'#sector-stock':{
    					click : me.stockGridView
    	    		}
    				});
    	 },
    	 onPanelRendered : function() {
             //just a console log to show when the panel si rendered
             console.log('The panel was rendered');
         },
    	    	stockGridView:function(){
    	    		alert("hi");
    	    	}*/
});